# This UI current has add staff, edit staff, delete and view all staff features.

# Instructions to run this front end:

open command line, cd into this project directory.
run npm install, then run npm start.
